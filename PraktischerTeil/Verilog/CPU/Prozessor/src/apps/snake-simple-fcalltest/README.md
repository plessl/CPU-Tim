Essentially same program as snake-simple, but with function calls instead of direct memory access.

This version requires that the data RAM access is fixed in the CPU FSM to correctly handle the two cycle latency.
